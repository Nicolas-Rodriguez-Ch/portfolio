export interface ExperienceObj {
  title: string;
  subtitle: string;
  description: string;
  dates: string;
}
export interface EducationObj {
  title: string;
  institution: string;
  year: number;
}
